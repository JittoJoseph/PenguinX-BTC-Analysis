
# ── Database ──────────────────────────────────────────────────
SUPABASE_DATABASE_URL=postgresql://user:password@host:5432/dbname

# ── Strategy (decided-window stale liquidity, BTC 15-minute Up/Down) ──
# The market and its 60s TWAP lookback are fixed in code, not configured here.
#
# Scan for entries between these two times before window close. Beyond 300s no
# margin is reliable enough to call a window decided; inside 5s the book has
# usually emptied and fills go partial.
ENTRY_WINDOW_OPEN_SECONDS=300
ENTRY_WINDOW_CLOSE_SECONDS=5
# Only buy the decided side when its executable ask sits within this band. The
# cap is what keeps the upside worth the downside: at 0.90 a win still returns
# 11%, and the stop can lose most of the stake.
MIN_ENTRY_PRICE=0.15
MAX_ENTRY_PRICE=0.90
# A window is decided when |forecast - strike| clears an empirical floor, in
# basis points of price by time-to-close, calibrated to 100% accuracy on real
# windows. This multiplies that floor; 1 is as calibrated, above 1 is stricter.
DECIDED_FLOOR_MULTIPLIER=1
# The floor is also raised to this many model standard deviations when that is
# larger, so volatile regimes demand more margin. It never lowers the floor.
DECIDED_SD_MULTIPLE=7
# Trailing window (ms) for per-second volatility of the RAW Chainlink feed.
SIGMA_WINDOW_MS=180000
# Refuse to trade when the raw feed is older than this; the roll-off term is
# only meaningful with a current spot price.
MAX_RAW_STALENESS_MS=5000
# Stop trigger, as a fraction of entry price. Always active — there is no way to
# turn it off. A fraction rather than a fixed number of cents because entries run
# from 0.15 to 0.90: 25c below entry is 28% of a 0.90 position, 83% of a 0.30
# one, and unreachable below 0.25, which would leave the cheapest positions with
# no stop at all. The trigger only decides WHEN to sell; the fill is whatever the
# book holds at that moment and can be far worse.
STOP_LOSS_FRACTION=0.35
# Simulated submit→match round-trip for exits; the book moves in between.
EXECUTION_LATENCY_MS=250
SCAN_INTERVAL_MS=60000
STARTING_CAPITAL=10

# ── API Server ────────────────────────────────────────────────
PORT=4000
HOST=0.0.0.0
LOG_LEVEL=info
NODE_ENV=production
ADMIN_PASSWORD=change_me_to_something_secure
